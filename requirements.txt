"""
Linux / modern Windows development dependencies (Python 3.10+).

    python3 -m venv venv
    source venv/bin/activate
    pip install -r requirements.txt
    python main.py

For offline Windows 8 packaging, use requirements-win8.txt instead
(Python 3.8–3.10 + PySide2).
"""

# GUI — Qt 6 (Linux and Windows 10+)
PySide6==6.11.1
shiboken6==6.11.1

# Database
SQLAlchemy==2.0.36
greenlet==3.1.1
typing_extensions==4.12.2

# Packaging helpers (optional on Linux)
pyinstaller>=6.0.0
pyinstaller-hooks-contrib>=2024.0
altgraph>=0.17.4
setuptools>=65.0.0
